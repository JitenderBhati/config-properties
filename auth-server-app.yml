server:
  port: 8383

  eureka:
    client:
      register-with-eureka: true

  spring:
    datasource:
      url: jdbc:mysql://localhost:3306/AuthServerDB
      username: root
      password: admin
      driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
      hibernate:
        ddl-auto: none
        naming:
          physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

    liquibase:
      change-log: classpath:db/changelog/changelog-master.xml

    h2:
      console:
        enabled: true

  logging:
    file:
      name: authserver.log
      path: ./
